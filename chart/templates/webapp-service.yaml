apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart.webapp.fullname" . }}
  labels:
    {{- include "chart.webapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.webapp.service.type }}
  ports:
    - port: {{ .Values.webapp.service.port }}
      targetPort: web
      protocol: TCP
      name: web
  selector:
    {{- include "chart.webapp.selectorLabels" . | nindent 4 }}
