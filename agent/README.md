# agent | k8s-images-manager

## Environemnt variables

- `NODE_NAME`: Name of the name, default to `Hostname`.
- `PUSH_FREQUENCY_IN_SECOND`: How much images list is push to the server, in second. Default to 30 seconds.
- `SERVER_URL`: Endpoint to reach the server
