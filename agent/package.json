{
  "name": "agent",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "cargo build",
    "watch": "SERVER_URL=http://127.0.0.1:9999 cargo run"
  },
  "devDependencies": {
    "nodemon": "3.1.14"
  }
}
